extends layout
block content
    h1 Welcome Post Page
    h1 #{post.name}
    h2 #{post.content}
    ol
        each comment in post.comment
            li #{comment.commentName}
            h1 #{comment.commentContent}
            h2 #{comment.userName}' tarafından yorum yapıldı